---
id: intro
title: Introduction
---

import { useState } from 'react';

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>
        Click me
      </button>
    </div>
  );
};

# Welcome to the JANI Ecosystem Documentation

This is a sample MDX file. You can write Markdown and use React components.

<Counter />
